# Mobile (Flutter) — placeholder

**Product:** wage-payroll  

The Flutter client is part of the stack (`docs/prompts/PROJECT-CONTEXT.md`) but is **not** generated in this foundation scaffold. Add a Flutter app under this folder (or a separate repo) per `docs/templates/6. MOBILE-PROMPT.md` when you start mobile work.

**API expectations:** short-lived access + refresh tokens over TLS; no cross-site cookie reliance for primary auth (see `docs/output/ARCHITECTURE-DEFINITION.md` session strategy).
